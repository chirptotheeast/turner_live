function numberChecker(value) {
  if (value === 1) {
    return "true";
  } else {
    return "false";
  }
}

function totalFinder(obj) {
  if (Object.keys(obj).includes("Total")) return obj;
}

function totalFinder(obj) {
  return { ...obj };
}

const Add = (num1, num2) => {
  return num1 + num2;
};

document.getElementById("operator-hours");

let buttonUpdate = document.getElementById("submit").alert();

buttonUpdate.onClick = alertDone;

function alertDone() {
  alert("Your information was sucesffuly submitted");
}

// onClick=